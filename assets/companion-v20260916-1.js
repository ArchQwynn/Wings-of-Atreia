(()=>{const KEY='woa:companion-characters:v1',MAX=10,stats=['STR','DEX','CON','INT','WIS','CHA'];let chars=JSON.parse(localStorage.getItem(KEY)||'[]'),current=null;const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];const base=()=>({id:crypto.randomUUID(),name:'',faction:'Elyos',class:'Aethertech',level:1,specialization:'',background:'',profession:'',kinah:0,prof:2,hp:0,maxHp:0,ac:10,initiative:0,speed:30,dp:0,flight:'',attacks:'',resources:'',weapon:'',shield:'',armor:'',wings:'',obelisk:'',implement:'',equipmentNotes:'',potions:'',ammo:'',consumables:'',inventoryNotes:'',inventory:'',spells:'',notes:'',scores:{STR:16,DEX:14,CON:14,INT:12,WIS:10,CHA:8}});function save(){localStorage.setItem(KEY,JSON.stringify(chars))}function mod(v){return Math.floor((Number(v)-10)/2)}function renderList(){current=null;$('#editorView').classList.add('hidden');$('#listView').classList.remove('hidden');$('#slotCount').textContent=`${chars.length} / ${MAX}`;$('#newBtn').disabled=chars.length>=MAX;$('#fullMsg').classList.toggle('hidden',chars.length<MAX);let box=$('#cards');box.innerHTML=chars.length?chars.map(c=>`<article class="card"><h3>${esc(c.name||'Unnamed Character')}</h3><div class="meta">Level ${c.level||1} • ${esc(c.class||'Class')} • ${esc(c.faction||'Faction')}</div><div class="numbers"><div class="number">HP<b>${c.hp||0}/${c.maxHp||0}</b></div><div class="number">AC<b>${c.ac||0}</b></div><div class="number">DP<b>${c.dp||0}/100</b></div></div><div class="cardactions"><button data-open="${c.id}">Open</button><button class="danger" data-del="${c.id}">Delete</button></div></article>`).join(''):`<div class="empty">No characters saved yet. Create your first character to begin.</div>`;$$('[data-open]').forEach(b=>b.onclick=()=>openChar(b.dataset.open));$$('[data-del]').forEach(b=>b.onclick=()=>deleteChar(b.dataset.del))}function esc(s){return String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}function openChar(id){current=chars.find(c=>c.id===id)||null;if(!current)return;showEditor()}function newChar(){if(chars.length>=MAX)return;current=base();showEditor()}function showEditor(){$('#listView').classList.add('hidden');$('#editorView').classList.remove('hidden');$('#editorTitle').textContent=current.name||'New Character';$$('[data-k]').forEach(el=>{let k=el.dataset.k;el.value=current[k]??''});let sg=$('#statsGrid');sg.innerHTML=stats.map(s=>`<label class="stat">${s}<input type="number" min="1" max="30" data-stat="${s}" value="${current.scores[s]??10}"><span>Modifier: <b id="mod-${s}">${mod(current.scores[s]??10)>=0?'+':''}${mod(current.scores[s]??10)}</b></span></label>`).join('');$$('[data-stat]').forEach(e=>e.oninput=()=>$('#mod-'+e.dataset.stat).textContent=(mod(e.value)>=0?'+':'')+mod(e.value));$('.tabpanel.active')?.classList.remove('active');$('#tab-overview').classList.add('active');$$('.tab').forEach(t=>t.classList.toggle('active',t.dataset.tab==='overview'));$('#saveNote').textContent=''}function read(){if(!current)return;$$('[data-k]').forEach(el=>{let k=el.dataset.k;current[k]=el.type==='number'?Number(el.value||0):el.value});current.scores={};$$('[data-stat]').forEach(e=>current.scores[e.dataset.stat]=Number(e.value||0));if(!current.name)current.name='Unnamed Character';current.level=Math.min(20,Math.max(1,current.level||1));return current}function persist(){read();let i=chars.findIndex(c=>c.id===current.id);if(i<0)chars.push(current);else chars[i]=current;save();$('#editorTitle').textContent=current.name;$('#saveNote').textContent='Saved on this device.'}function deleteChar(id=current?.id){let c=chars.find(x=>x.id===id);if(!c)return;if(confirm(`Delete “${c.name||'Unnamed Character'}”? This cannot be undone unless you exported it.`)){chars=chars.filter(x=>x.id!==id);save();renderList()}}$('#newBtn').onclick=newChar;$('#backBtn').onclick=()=>{if(current){read();}renderList()};$('#saveBtn').onclick=persist;$('#deleteBtn').onclick=()=>deleteChar();$('#clearAll').onclick=()=>{if(chars.length&&confirm('Delete all saved characters from this device?')){chars=[];save();renderList()}};$('#exportAll').onclick=()=>{const blob=new Blob([JSON.stringify({format:'WoA Companion Character Book v1',characters:chars},null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='woa-companion-characters.json';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),500)};$('#importAll').onclick=()=>$('#importFile').click();$('#importFile').onchange=e=>{let f=e.target.files[0];if(!f)return;let r=new FileReader();r.onload=()=>{try{let d=JSON.parse(r.result),incoming=Array.isArray(d)?d:d.characters;if(!Array.isArray(incoming))throw 0;let room=MAX-chars.length;if(incoming.length>room){alert(`Only ${room} character slot(s) are available. Delete characters before importing more.`);return}chars.push(...incoming.slice(0,room).map(x=>({...base(),...x,id:crypto.randomUUID(),scores:{...base().scores,...(x.scores||{})}})));save();renderList()}catch{alert('That file is not a valid Wings of Atreia character export.')}};r.readAsText(f);e.target.value=''};$$('.tab').forEach(t=>t.onclick=()=>{$$('.tab').forEach(x=>x.classList.remove('active'));$$('.tabpanel').forEach(x=>x.classList.remove('active'));t.classList.add('active');$('#tab-'+t.dataset.tab).classList.add('active')});renderList()})();
